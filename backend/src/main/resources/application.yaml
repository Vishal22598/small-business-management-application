spring:
  application:
    name: small-business-management

  datasource:
    url: jdbc:mysql://localhost:3306/shop_management_system
    username: vishal
    password: vishal

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  jackson:
    time-zone: Asia/Kolkata